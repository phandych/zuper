<li>
	<%= product.name %>
	<%= form_for(@cart_item) do |f| %>
		<%= hidden_field_tag :product_id, product.id %>
		<%= f.hidden_field :quantity, value: 1 %>
		<%= f.hidden_field :product_id, value: product.id %>
		<%= f.submit "Add to Cart", class: "btn btn-primary add_to_cart" %>
	<% end %>
</li>